// Create a function that takes a number argument and 
// returns true if the number is negative, false otherwise.

function isNegative(value) {
    if (value < 0) {
        return true;
    } else if (value >= 0) {
        return false;
    }
}